import { NextResponse } from "next/server"; import { connectToDatabase } from "@/lib/mongodb"; import { Job } from "@/models"; import { enqueueJob } from "@/lib/jobs";
export async function GET() { await connectToDatabase(); return NextResponse.json(await Job.find().sort({ createdAt: -1 }).limit(100).lean()); }
export async function POST(request: Request) { try { const body = await request.json(); if (!body.type || typeof body.type !== "string") return NextResponse.json({ error: "type is required" }, { status: 400 }); return NextResponse.json(await enqueueJob(body.type, body.payload || {}, Number(body.priority || 0)), { status: 202 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to enqueue job" }, { status: 400 }); } }
