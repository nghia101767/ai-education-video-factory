import { NextResponse } from "next/server"; import { isResource, validatePayload } from "@/lib/content"; import { deleteContent, getContent, updateContent } from "@/repositories/content-repository"; type Context = { params: Promise<{ resource: string; id: string }> };
export async function GET(_: Request, { params }: Context) { const { resource, id } = await params; if (!isResource(resource)) return NextResponse.json({ error: "Unknown resource" }, { status: 404 }); const item = await getContent(resource, id); return item ? NextResponse.json(item) : NextResponse.json({ error: "Not found" }, { status: 404 }); }
export async function PATCH(request: Request, { params }: Context) { const { resource, id } = await params; if (!isResource(resource)) return NextResponse.json({ error: "Unknown resource" }, { status: 404 }); try { const item = await updateContent(resource, id, validatePayload(resource, await request.json(), true)); return item ? NextResponse.json(item) : NextResponse.json({ error: "Not found" }, { status: 404 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to update" }, { status: 400 }); } }
export const PUT = PATCH;
export async function DELETE(_: Request, { params }: Context) { const { resource, id } = await params; if (!isResource(resource)) return NextResponse.json({ error: "Unknown resource" }, { status: 404 }); try { const item = await deleteContent(resource, id); return item ? NextResponse.json({ deleted: true }) : NextResponse.json({ error: "Not found" }, { status: 404 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to delete" }, { status: 409 }); } }
