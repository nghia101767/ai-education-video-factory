export function toSrtTime(seconds: number) { const ms = Math.max(0, Math.round(seconds * 1000)); const h = Math.floor(ms / 3600000); const m = Math.floor((ms % 3600000) / 60000); const s = Math.floor((ms % 60000) / 1000); return `${String(h).padStart(2, "0")}:${String(m).padStart(2, "0")}:${String(s).padStart(2, "0")},${String(ms % 1000).padStart(3, "0")}`; }
export function createSrt(lines: Array<{ text: string; start: number; end: number }>) { return lines.map((line, index) => `${index + 1}\n${toSrtTime(line.start)} --> ${toSrtTime(line.end)}\n${line.text.trim()}\n`).join("\n"); }
